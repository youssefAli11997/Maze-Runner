package characters.players.types;

import characters.players.Player;

public class Man extends Player {
	public Man(int currentRow, int currentColumn, int gridRows, int gridColumns) {
		super(currentRow, currentColumn, gridRows, gridColumns);
		super.setPlayerName("Deafult man");//deafult name
	}
	
}
